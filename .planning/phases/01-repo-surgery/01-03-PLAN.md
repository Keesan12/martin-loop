---
id: "01-03"
title: "README Update with Canonical Runtime Path Map"
phase: 01-repo-surgery
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - README.md
requirements: [R1.7, R1.8]
autonomous: true

must_haves:
  truths:
    - "README.md lists all 5 canonical runtime files with their exact paths"
    - "An engineer unfamiliar with the repo can identify live code paths within 10 minutes by reading the README"
    - "README reflects actual project state (pnpm, not npm; correct package descriptions)"
  artifacts:
    - path: "README.md"
      provides: "Updated project README with canonical runtime path map"
      contains: "packages/contracts/src/index.ts"
  key_links:
    - from: "README.md"
      to: "packages/contracts/src/index.ts"
      via: "documented path reference"
      pattern: "packages/contracts/src/index.ts"
    - from: "README.md"
      to: "packages/core/src/index.ts"
      via: "documented path reference"
      pattern: "packages/core/src/index.ts"
    - from: "README.md"
      to: "packages/adapters/src/claude-cli.ts"
      via: "documented path reference"
      pattern: "packages/adapters/src/claude-cli.ts"
    - from: "README.md"
      to: "packages/cli/src/index.ts"
      via: "documented path reference"
      pattern: "packages/cli/src/index.ts"
    - from: "README.md"
      to: "packages/mcp/src/server.ts"
      via: "documented path reference"
      pattern: "packages/mcp/src/server.ts"
---

<objective>
Rewrite the root README.md to prominently feature the 5 canonical runtime files, reflect the correct package manager (pnpm), and give an unfamiliar engineer a clear map of live code paths within 10 minutes.

Purpose: The current README is accurate in content but does not highlight the canonical runtime files that define where the real code lives. R1.7 requires the README to explicitly list these 5 files. R1.8 requires that an unfamiliar engineer can orient themselves quickly. The README is the first thing anyone reads — it must be a reliable map.

Output: Updated README.md committed to rebuild/v4-controller branch.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README.md with canonical runtime path map</name>
  <files>README.md</files>
  <read_first>
    - README.md
    - .planning/PROJECT.md
    - packages/contracts/src/index.ts
    - packages/core/src/index.ts
    - packages/adapters/src/claude-cli.ts
    - packages/cli/src/index.ts
    - packages/mcp/src/server.ts
  </read_first>
  <action>
Rewrite README.md. Keep the useful content from the existing README (CLI usage, MCP server docs, environment variables, testing commands) but restructure it so that:

1. A new section titled `## Canonical Runtime Files` appears immediately after the introductory "What it does" section and BEFORE the Packages table.

2. This section contains the following exact content:

```markdown
## Canonical Runtime Files

These are the 5 entry points that define Martin's live code. Start here when navigating the codebase:

| # | File | Role |
|---|------|------|
| 1 | `packages/contracts/src/index.ts` | All shared types (LoopRecord, LoopBudget, events) |
| 2 | `packages/core/src/index.ts` | Orchestration engine (runMartin, exit intelligence) |
| 3 | `packages/adapters/src/claude-cli.ts` | Claude CLI adapter implementation |
| 4 | `packages/cli/src/index.ts` | CLI entry point (martin run/inspect/bench) |
| 5 | `packages/mcp/src/server.ts` | MCP server (martin_run, martin_inspect tools) |
```

3. Update the "Installation" section to use `pnpm` consistently (the current README already uses pnpm, but verify no `npm` commands crept in except for global installs of external tools like claude-code and codex which are correctly npm).

4. Add a new section `## Project Structure` (replace or enhance the existing "Monorepo structure" section) that shows:

```markdown
## Project Structure

```
martin-loop/
├── packages/
│   ├── contracts/     # Shared types and event schemas
│   ├── core/          # Orchestration engine (runMartin)
│   ├── adapters/      # Claude CLI, Codex CLI, stub adapters
│   ├── cli/           # martin CLI binary
│   └── mcp/           # MCP server
├── apps/
│   ├── control-plane/ # Next.js governance dashboard
│   └── local-dashboard/ # Express + vanilla JS local UI
├── benchmarks/        # Eval harness and fixture tasks
├── docs/
│   └── legacy/        # Historical reference material
├── scripts/           # Build and utility scripts
├── deploy/            # Deployment configuration
└── .planning/         # GSD project planning (rebuild roadmap)
```
```

5. Keep ALL existing sections that are still accurate:
   - "What it does" (intro section)
   - "Prerequisites"
   - "Installation"
   - "CLI Usage" with the full flags table
   - "Config file" section
   - "MCP Server" section
   - "Environment variables"
   - "Testing"
   - "Publishing"

6. Remove or do not include any reference to the V3 directory, zip files, or legacy docs at the root (those have been relocated/ignored).

7. The Packages table should remain but come AFTER the Canonical Runtime Files section.

8. Do NOT change the content of CLI flags, MCP tool examples, or config examples — they are accurate.

After rewriting, stage and commit:

```bash
git add README.md
git commit -m "docs(01-03): update README with canonical runtime path map

Lists all 5 canonical runtime files prominently.
Updated project structure to reflect docs/legacy/ and current layout.
Satisfies R1.7 and R1.8."
```
  </action>
  <verify>
    <automated>cd "C:/Users/Torram/OneDrive/Documents/Codex Main/Setup Stuff/martin-loop" && echo "=== Canonical files check ===" && grep -c "packages/contracts/src/index.ts" README.md && grep -c "packages/core/src/index.ts" README.md && grep -c "packages/adapters/src/claude-cli.ts" README.md && grep -c "packages/cli/src/index.ts" README.md && grep -c "packages/mcp/src/server.ts" README.md && echo "=== Section header check ===" && grep -c "## Canonical Runtime Files" README.md && echo "=== docs/legacy in structure ===" && grep -c "docs/legacy" README.md && echo "=== Commit check ===" && git log --oneline -1</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains the exact string `## Canonical Runtime Files` as a section header
    - README.md contains the exact string `packages/contracts/src/index.ts` at least once
    - README.md contains the exact string `packages/core/src/index.ts` at least once
    - README.md contains the exact string `packages/adapters/src/claude-cli.ts` at least once
    - README.md contains the exact string `packages/cli/src/index.ts` at least once
    - README.md contains the exact string `packages/mcp/src/server.ts` at least once
    - README.md contains a reference to `docs/legacy` in the project structure
    - The `## Canonical Runtime Files` section appears BEFORE the `## Packages` section (check line numbers: Canonical line number < Packages line number)
    - README.md still contains `## CLI Usage` section
    - README.md still contains `## MCP Server` section
    - README.md still contains `## Testing` section
    - `git log --oneline -1` shows commit message containing "canonical runtime path map"
    - All 5 grep commands for canonical file paths return >= 1
  </acceptance_criteria>
  <done>README.md prominently lists all 5 canonical runtime files. Project structure section reflects current repo layout including docs/legacy/. An unfamiliar engineer can read the Canonical Runtime Files table and know exactly where to start. Satisfies R1.7 (canonical paths in README) and R1.8 (10-minute orientation).</done>
</task>

</tasks>

<verification>
1. `grep "## Canonical Runtime Files" README.md` returns a match
2. All 5 canonical file paths appear in README.md (verified by grep)
3. `grep "docs/legacy" README.md` returns a match
4. README.md retains CLI usage, MCP server, testing sections
5. `git log --oneline` shows the README commit on rebuild/v4-controller
6. No references to V3 directory or zip files in README.md
</verification>

<success_criteria>
- README.md has a prominent "Canonical Runtime Files" section listing all 5 entry points
- Project structure section accurately reflects the current repo layout
- An engineer reading only the README can identify where live code lives within minutes
- All existing accurate documentation (CLI, MCP, testing) is preserved
- Satisfies R1.7 (canonical paths documented) and R1.8 (10-minute orientation)
</success_criteria>

<output>
After completion, create `.planning/phases/01-repo-surgery/01-03-SUMMARY.md`
</output>
