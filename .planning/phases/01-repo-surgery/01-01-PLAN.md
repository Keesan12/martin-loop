---
id: "01-01"
title: "Git Init, Branch Setup, and Gitignore Hardening"
phase: 01-repo-surgery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .git/
  - .gitignore
requirements: [R1.1, R1.3, R1.6]
autonomous: true

must_haves:
  truths:
    - "martin-loop has its own standalone git repository"
    - "rebuild/v4-controller branch exists and is checked out"
    - ".gitignore blocks all generated artifacts and noise from being tracked"
    - "No zip archives, node_modules, dist, .next, .turbo, .npm-cache, or vitest-results.json appear in git status"
  artifacts:
    - path: ".git/"
      provides: "Standalone git repository for martin-loop"
    - path: ".gitignore"
      provides: "Comprehensive ignore rules for all generated/noise files"
      contains: "*.zip"
  key_links:
    - from: ".gitignore"
      to: "git status"
      via: "gitignore rules filter tracked files"
      pattern: "nothing to commit"
---

<objective>
Initialize a standalone git repository inside the martin-loop directory, create the rebuild/v4-controller branch, and harden the .gitignore so that all generated artifacts, zip archives, and cache directories are excluded from tracking.

Purpose: martin-loop currently has no git repo of its own (it sits untracked inside a parent repo). Phase 1 requires a clean branch as the foundation for the entire rebuild. The gitignore must be bulletproof before the initial commit so noise never enters history.

Output: A git-initialized martin-loop with rebuild/v4-controller branch checked out. Hardened .gitignore committed as the first commit on main, branch created from that.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden .gitignore with comprehensive ignore rules</name>
  <files>.gitignore</files>
  <read_first>
    - .gitignore
  </read_first>
  <action>
Overwrite `.gitignore` with the following exact content. This covers every known noise category in the martin-loop repo:

```gitignore
# Dependencies
node_modules/

# Build output
dist/
coverage/
.next/
.turbo/
*.tsbuildinfo

# Environment
.env
.env.local
.env.*.local

# Archives (never track zip/tar artifacts)
*.zip
*.tar.gz
*.tgz

# Cache directories
.npm-cache/
.cache/

# Test output
vitest-results.json
*.sqlite

# Runtime artifacts
artifacts/
output/

# OS files
.DS_Store
Thumbs.db
```

This adds the following missing entries compared to the current .gitignore:
- `*.zip` (blocks martin-cli-dist.zip, martin-loop-production-final.zip, martin-loop-remediation-pack-v3.zip, martin-loop-remediation-pack-v4.zip, martin-mcp-dist.zip)
- `.npm-cache/` (blocks the npm cache directory at root)
- `output/` (blocks the output directory at root)
- `vitest-results.json` (blocks test result artifact)
- `.env.*.local` (broader env protection)
- `*.tar.gz`, `*.tgz` (future-proof archive blocking)
- `.cache/`, `.DS_Store`, `Thumbs.db` (standard noise)

Do NOT remove any existing entries. The final file must contain ALL of the above lines.
  </action>
  <verify>
    <automated>cd "C:/Users/Torram/OneDrive/Documents/Codex Main/Setup Stuff/martin-loop" && grep -c "^\*\.zip$" .gitignore && grep -c "^\.npm-cache/" .gitignore && grep -c "^output/" .gitignore && grep -c "^vitest-results.json$" .gitignore && grep -c "^node_modules/" .gitignore && grep -c "^dist/" .gitignore && grep -c "^\.next/" .gitignore && grep -c "^\.turbo/" .gitignore</automated>
  </verify>
  <acceptance_criteria>
    - `.gitignore` contains the line `*.zip` (exact match)
    - `.gitignore` contains the line `.npm-cache/` (exact match)
    - `.gitignore` contains the line `output/` (exact match)
    - `.gitignore` contains the line `vitest-results.json` (exact match)
    - `.gitignore` retains `node_modules/`, `dist/`, `coverage/`, `.next/`, `.turbo/`, `.env`, `.env.local`, `*.tsbuildinfo`, `*.sqlite`, `artifacts/`
    - All 8 grep commands in verify return `1` (each pattern found exactly once)
  </acceptance_criteria>
  <done>.gitignore contains all original entries plus *.zip, .npm-cache/, output/, vitest-results.json, and other noise patterns. File is ready for the initial commit.</done>
</task>

<task type="auto">
  <name>Task 2: Initialize git repo, commit on main, create rebuild/v4-controller branch</name>
  <files>.git/</files>
  <read_first>
    - .gitignore
  </read_first>
  <action>
Run the following commands in sequence inside the martin-loop directory (`C:/Users/Torram/OneDrive/Documents/Codex Main/Setup Stuff/martin-loop/`):

1. Initialize a new git repo:
   ```bash
   git init
   ```

2. Stage the hardened .gitignore (and ONLY .gitignore for the initial commit):
   ```bash
   git add .gitignore
   ```

3. Create the initial commit on main (this is the default branch):
   ```bash
   git commit -m "chore: initialize repo with hardened .gitignore"
   ```

4. Create and checkout the rebuild/v4-controller branch:
   ```bash
   git checkout -b rebuild/v4-controller
   ```

5. Verify the branch is active:
   ```bash
   git branch --show-current
   ```
   Expected output: `rebuild/v4-controller`

6. Verify that noise files do NOT appear in git status (they should be ignored):
   ```bash
   git status
   ```
   The zip files, node_modules/, dist/, .npm-cache/, output/, vitest-results.json should NOT appear as untracked files.

IMPORTANT: The initial commit must contain ONLY .gitignore. All other files will be staged and committed in subsequent plans (01-02 and 01-03) after cleanup is complete. This prevents noise from ever entering git history.

IMPORTANT: Do NOT run `git add .` or `git add -A`. Only stage `.gitignore` for this initial commit.
  </action>
  <verify>
    <automated>cd "C:/Users/Torram/OneDrive/Documents/Codex Main/Setup Stuff/martin-loop" && git branch --show-current && git log --oneline -1 && git status --short | grep -v "^??" | head -5; echo "---untracked noise check---"; git status --short | grep -c "\.zip$" || echo "0 zip files in status (good)"</automated>
  </verify>
  <acceptance_criteria>
    - `git branch --show-current` outputs exactly `rebuild/v4-controller`
    - `git log --oneline -1` shows a commit with message containing "hardened .gitignore"
    - `git status` does NOT list any `.zip` files as untracked
    - `git status` does NOT list `node_modules/` as untracked
    - `git status` does NOT list `dist/` as untracked
    - `git status` does NOT list `.npm-cache/` as untracked
    - `git status` does NOT list `vitest-results.json` as untracked
    - `git status` does NOT list `output/` as untracked
    - `.git/` directory exists inside martin-loop
  </acceptance_criteria>
  <done>Git repo initialized in martin-loop. Initial commit on main with hardened .gitignore. rebuild/v4-controller branch created and checked out. All noise files are excluded from tracking by .gitignore.</done>
</task>

</tasks>

<verification>
1. `.git/` directory exists at martin-loop root
2. `git branch --show-current` returns `rebuild/v4-controller`
3. `git log --oneline` shows at least one commit
4. `.gitignore` contains entries for: `*.zip`, `.npm-cache/`, `output/`, `vitest-results.json`, `node_modules/`, `dist/`, `.next/`, `.turbo/`, `*.tsbuildinfo`, `*.sqlite`, `artifacts/`, `.env`, `.env.local`
5. `git status` shows no zip files, no node_modules, no dist, no .npm-cache as untracked
</verification>

<success_criteria>
- martin-loop has a standalone git repository (not dependent on parent Setup Stuff repo)
- rebuild/v4-controller branch exists and is the current branch
- .gitignore is hardened to exclude all known noise categories
- Zero noise files appear in `git status` output
- Satisfies R1.1 (branch exists), R1.3 (generated artifacts excluded), R1.6 (strict .gitignore)
</success_criteria>

<output>
After completion, create `.planning/phases/01-repo-surgery/01-01-SUMMARY.md`
</output>
