---
id: "01-02"
title: "Root Cleanup — Remove Noise, Relocate Legacy Docs"
phase: 01-repo-surgery
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - docs/legacy/ENGINEERING.md
  - docs/legacy/HANDOVER.md
  - docs/legacy/OPEN-ME-FIRST.html
requirements: [R1.2, R1.4, R1.5]
autonomous: true

must_haves:
  truths:
    - "No zip archives exist in tracked git paths"
    - "MartinLoop V3 3-31-2026/ directory is not in tracked paths"
    - "Legacy reference documents are organized in docs/legacy/ and accessible"
    - "Root directory contains only canonical project files"
  artifacts:
    - path: "docs/legacy/ENGINEERING.md"
      provides: "Relocated engineering reference doc"
    - path: "docs/legacy/HANDOVER.md"
      provides: "Relocated handover reference doc"
    - path: "docs/legacy/OPEN-ME-FIRST.html"
      provides: "Relocated onboarding reference doc"
  key_links:
    - from: "docs/legacy/"
      to: "root directory"
      via: "git mv moves files, originals removed from root"
      pattern: "docs/legacy/ENGINEERING.md"
---

<objective>
Clean the martin-loop root directory by ensuring noise files (zips, historical directories) are excluded from git tracking and relocating legacy reference documents to docs/legacy/.

Purpose: The repo root is cluttered with 5 zip archives, a historical V3 directory, and legacy docs that obscure the actual project structure. An engineer looking at the root should see only canonical project files. This directly satisfies the "identify live code in under 10 minutes" requirement.

Output: Clean root with legacy docs relocated to docs/legacy/. Noise files excluded via .gitignore (from Plan 01-01). Committed to rebuild/v4-controller branch.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-repo-surgery/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify noise files are git-ignored and relocate legacy docs to docs/legacy/</name>
  <files>
    docs/legacy/ENGINEERING.md
    docs/legacy/HANDOVER.md
    docs/legacy/OPEN-ME-FIRST.html
  </files>
  <read_first>
    - .gitignore
    - ENGINEERING.md
    - HANDOVER.md
    - OPEN-ME-FIRST.html
  </read_first>
  <action>
All commands run from the martin-loop root: `C:/Users/Torram/OneDrive/Documents/Codex Main/Setup Stuff/martin-loop/`

**Step 1: Verify noise exclusion (no action needed, just confirm .gitignore from Plan 01-01 works)**

Run `git status` and confirm that NONE of the following appear as untracked files:
- `martin-cli-dist.zip`
- `martin-loop-production-final.zip`
- `martin-loop-remediation-pack-v3.zip`
- `martin-loop-remediation-pack-v4.zip`
- `martin-mcp-dist.zip`
- `node_modules/`
- `dist/`
- `.npm-cache/`
- `output/`
- `vitest-results.json`

These files remain on disk but are invisible to git because of the hardened .gitignore from Plan 01-01. Do NOT delete them from disk — they are just excluded from tracking.

Also confirm that `MartinLoop V3 3-31-2026/` does NOT appear in `git status`. Since this directory was never tracked (the repo was just initialized in Plan 01-01), it should be untracked. However, it should still show up in `git status` as an untracked directory because .gitignore does not have a rule for it. We handle this in the next step.

**Step 2: Add `MartinLoop V3 3-31-2026/` to .gitignore**

Append the following line to `.gitignore`:

```
# Historical archive (not part of v4 rebuild)
MartinLoop V3 3-31-2026/
```

This ensures the V3 directory is never accidentally tracked. After adding, run `git status` to confirm it no longer appears.

**Step 3: Add `demo/` to .gitignore**

The `demo/` directory at root is not a canonical project directory. Append to `.gitignore`:

```
# Legacy demo directory
demo/
```

**Step 4: Create docs/legacy/ directory and move legacy reference documents**

```bash
mkdir -p docs/legacy
```

Move the following three files from the repo root into docs/legacy/:

```bash
mv ENGINEERING.md docs/legacy/ENGINEERING.md
mv HANDOVER.md docs/legacy/HANDOVER.md
mv OPEN-ME-FIRST.html docs/legacy/OPEN-ME-FIRST.html
```

Use regular `mv` (not `git mv`) since these files have never been committed to git yet (the repo was just initialized with only .gitignore committed).

**Step 5: Stage and commit**

```bash
git add .gitignore docs/legacy/ENGINEERING.md docs/legacy/HANDOVER.md docs/legacy/OPEN-ME-FIRST.html
git commit -m "chore(01-02): relocate legacy docs to docs/legacy, exclude V3 dir and demo from tracking"
```

Do NOT use `git add .` or `git add -A`. Only stage the specific files listed above.
  </action>
  <verify>
    <automated>cd "C:/Users/Torram/OneDrive/Documents/Codex Main/Setup Stuff/martin-loop" && echo "=== docs/legacy contents ===" && ls docs/legacy/ && echo "=== root legacy check ===" && test ! -f ENGINEERING.md && echo "ENGINEERING.md not at root (good)" || echo "ENGINEERING.md still at root (BAD)" && test ! -f HANDOVER.md && echo "HANDOVER.md not at root (good)" || echo "HANDOVER.md still at root (BAD)" && test ! -f OPEN-ME-FIRST.html && echo "OPEN-ME-FIRST.html not at root (good)" || echo "OPEN-ME-FIRST.html still at root (BAD)" && echo "=== gitignore V3 check ===" && grep -c "MartinLoop V3" .gitignore && echo "=== git status noise check ===" && git status --short | grep -c "\.zip" && echo "(should be 0)"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/legacy/ENGINEERING.md` exists and has non-zero content
    - `docs/legacy/HANDOVER.md` exists and has non-zero content
    - `docs/legacy/OPEN-ME-FIRST.html` exists and has non-zero content
    - `ENGINEERING.md` does NOT exist at the repo root
    - `HANDOVER.md` does NOT exist at the repo root
    - `OPEN-ME-FIRST.html` does NOT exist at the repo root
    - `.gitignore` contains the line `MartinLoop V3 3-31-2026/`
    - `.gitignore` contains the line `demo/`
    - `git status` does NOT show any `.zip` files
    - `git status` does NOT show `MartinLoop V3 3-31-2026/` as untracked
    - `git log --oneline` shows a commit with message containing "relocate legacy docs"
    - The zip files still exist on disk (not deleted, just ignored): `ls *.zip` returns 5 files
  </acceptance_criteria>
  <done>Root is clean. Legacy docs live in docs/legacy/. All noise (zips, V3 dir, demo, node_modules, dist, caches) is excluded from git tracking via .gitignore. Satisfies R1.2 (no zips tracked), R1.4 (V3 dir excluded), R1.5 (legacy docs relocated).</done>
</task>

</tasks>

<verification>
1. `ls docs/legacy/` returns ENGINEERING.md, HANDOVER.md, OPEN-ME-FIRST.html
2. `ls ENGINEERING.md HANDOVER.md OPEN-ME-FIRST.html 2>&1` returns "No such file" for all three
3. `git status` output contains zero zip files
4. `git status` output does not contain "MartinLoop V3"
5. `grep "MartinLoop V3" .gitignore` returns a match
6. `git log --oneline` shows the cleanup commit
7. Zip files still exist on disk: `ls *.zip | wc -l` returns 5
</verification>

<success_criteria>
- Root directory contains only canonical project files (README.md, package.json, config files, packages/, apps/, etc.)
- Legacy docs are preserved and accessible in docs/legacy/
- No zip archives appear in any git-tracked path
- MartinLoop V3 3-31-2026/ is excluded from git tracking
- Satisfies R1.2 (no zips in tracked paths), R1.4 (V3 dir excluded), R1.5 (legacy docs relocated)
</success_criteria>

<output>
After completion, create `.planning/phases/01-repo-surgery/01-02-SUMMARY.md`
</output>
