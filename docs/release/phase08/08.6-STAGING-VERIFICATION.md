# Phase 08.6 Internal OSS Staging Verification

Date: 2026-06-08  
Branch: `codex/gsd-phase08-stage`

## Hard gates (all green)

Executed on this exact branch head:

```bash
pnpm public:copy-scan
pnpm public:git-surface
pnpm test
pnpm build
pnpm release:validate-local
pnpm release:matrix:local
pnpm --filter @martinloop/mcp verify:release
pnpm mcp:published:smoke:pack
```

Results: all commands exited successfully.

## Additional quality evidence

- Workspace CLI test suite: `pnpm --filter @martin/cli test` passed after onboarding command additions.
- Phase 08 trust/runtime deltas remained green in full workspace tests.
- MCP packaged smoke and discovery checks passed with current staged surface.

## Public-surface hygiene

- Public copy scan passed.
- Public git-surface guard passed.
- Docs updates remain public-facing and avoid internal planning leakage.

## Release truth note

Published `martin-loop@0.3.2` validates governed baseline flow (`doctor/session-start/preflight/run/share`).  
New onboarding commands (`start`, `enable`, `env`, `review`, `receipts explain`) are verified in this staging branch and require promotion to public release to become npm-visible.

