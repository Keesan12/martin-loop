# Phase 08.5 Red-Team Report

Date: 2026-06-08
Branch: `codex/gsd-phase08-stage`

## Scope

Trust/runtime hardening and onboarding command surface for Phase 08:

- selector canonicalization across run inspection commands
- verifier truth model (`passed|failed|contradicted|not_run`)
- integrity-state machine (`verified|unsigned|tamper_detected|relocated|material_missing|selector_noncanonical`)
- trust rendering in dossier/share/proof surfaces
- onboarding commands: `start`, `enable`, `env`, `review`, `receipts explain`, objective shorthand

## Adversarial checks executed

1. Tampered canonical run record:
- Expected: integrity `tamper_detected`, trust warnings, no verified trust claim.
- Result: pass (covered in CLI/MCP operator suites and full workspace test run).

2. Relocated evidence loaded outside canonical runs root:
- Expected: integrity `relocated`; no canonical trust claim.
- Result: pass.

3. Missing integrity material:
- Expected: integrity `material_missing`; verifier evidence treated as untrusted.
- Result: pass.

4. Non-canonical selector trust degradation:
- Expected: `selector_noncanonical` state when canonical integrity checks are not possible.
- Result: pass.

5. Verification contradiction classification:
- Expected: `contradicted` status instead of false clean-pass framing.
- Result: pass.

6. Share bundle trust-label integrity:
- Expected: weak integrity states degrade trust copy and warnings in share outputs.
- Result: pass.

## Real E2E evidence (clean temp runs)

### Published package lane (`martin-loop@0.3.2`)

Executed from clean temp directory:

- `npx -y martin-loop@0.3.2 doctor --json`
- `npx -y martin-loop@0.3.2 session-start --json`
- `npx -y martin-loop@0.3.2 preflight ... --json`
- `npx -y martin-loop@0.3.2 run ... --proof --json`
- `npx -y martin-loop@0.3.2 dossier --latest --json`
- `npx -y martin-loop@0.3.2 share --latest --json`

Observed:

- lifecycle: `completed`
- verification: `passed`
- share bundle generated with receipt artifacts
- integrity in this no-signing test lane reported as `unsigned` (expected posture for that run shape)

Temp evidence root:

- `C:\Users\Torram\AppData\Local\Temp\martin-e2e-published-ff314a59`

### Candidate branch lane (new onboarding surface)

Executed from clean temp directory using staged local CLI build:

- `node dist/bin/martin-loop.js start --json`
- `node dist/bin/martin-loop.js env --json`
- `node dist/bin/martin-loop.js enable ... --json`
- governed proof flow via objective shorthand
- `node dist/bin/martin-loop.js review --json`
- `node dist/bin/martin-loop.js receipts explain --latest --json`
- `node dist/bin/martin-loop.js share --latest --json`

Observed:

- lifecycle: `completed`
- review integrity: `verified`
- receipts explain integrity: `verified`
- share bundle generated and inspectable

Temp evidence root:

- `C:\Users\Torram\AppData\Local\Temp\martin-e2e-candidate-47d9962b`

## Security posture notes

- No silent global interception behavior was introduced.
- `enable` writes repo-local config only.
- Trust states are explicit and non-upgraded on weak evidence paths.
- Share artifacts remain path-redacted for public-safe exchange.

