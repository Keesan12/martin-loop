# @martinloop/mcp 0.6.3

The 0.6.3 MCP release stays version-aligned with MartinLoop 0.6.3 and the receipt-integrity hotfix.

## What changed

- Package, server, runtime, plugin, and MCPB product versions align at 0.6.3.
- MCP install metadata points at the exact `@martinloop/mcp@0.6.3` package line.
- MCPB manifest schema remains 0.3.
- The MCP host contract is unchanged; this release keeps the governed execution surface aligned with the integrity hotfix CLI release.

```sh
npx -y @martinloop/mcp@0.6.3
```
