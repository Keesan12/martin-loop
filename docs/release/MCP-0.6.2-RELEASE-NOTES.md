# @martinloop/mcp 0.6.2

The 0.6.2 MCP release stays version-aligned with MartinLoop 0.6.2 and the Codex provenance hardening release.

## What changed

- Package, server, runtime, plugin, and MCPB product versions align at 0.6.2.
- MCP install metadata points at the exact `@martinloop/mcp@0.6.2` package line.
- MCPB manifest schema remains 0.3.
- The MCP host contract is unchanged; this release keeps the governed execution surface aligned with the hardened CLI release.

```sh
npx -y @martinloop/mcp@0.6.2
```
