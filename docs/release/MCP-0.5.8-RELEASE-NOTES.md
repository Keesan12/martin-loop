# @martinloop/mcp 0.5.8

This release keeps the MCP package, server metadata, plugin metadata, and MCPB product version aligned with the MartinLoop 0.5.8 corrective release.

## Changes

- Aligns package, server initialization, plugin, MCPB product, and install metadata at `0.5.8`.
- Carries forward the MCP schema and registry hardening from `0.5.7`.
- Preserves the MCPB manifest schema at `0.3`; `0.5.8` is the product version.
