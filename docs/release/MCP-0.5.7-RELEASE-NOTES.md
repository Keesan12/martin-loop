# @martinloop/mcp 0.5.7

This release hardens MCP tool schemas for registry compliance and aligns all distribution metadata at `0.5.7`.

## Changes

- Hardens MCP tool input schemas and descriptions for Glama registry A-grade compliance.
- Keeps package, server initialization, plugin, MCPB product, and install metadata aligned at `0.5.7`.
- OSS boundary allow-list updated to include `llms.txt` and `llms-full.txt` discovery files.
- Preserves one governed execution authority and receipt integrity chain semantics.

The MCPB manifest schema remains `0.3`; `0.5.7` is the product version.
