# @martinloop/mcp 0.6.7 — Version Alignment Patch

## What changed

The standalone MCP package remains behaviorally compatible with 0.6.6 and advances with the aligned MartinLoop 0.6.7 release train. This patch does not change MCP tool schemas or runtime selector behavior.
