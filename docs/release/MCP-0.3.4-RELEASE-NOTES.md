# @martinloop/mcp 0.3.4

`0.3.4` rewrites the npm README to lead with the problem and evidence, not internal release tracking. Also aligns MCP package version with the published root CLI.

## What Changed

- Rewrote the npm README from scratch: leads with the cost problem, shows real numbers from production testing ($28 overshoot on a $1.50 budget), documents all 21 tools and 11 resources, and speaks to both technical and non-technical audiences.
- Bumped MCP package version to `0.3.4` to align with the published version line.
- Updated `server.json` version parity.

## Quick Check

```sh
npx -y @martinloop/mcp --help
```
