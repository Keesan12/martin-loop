# Handoff — 2026-06-24 (End of Session)

## What shipped today

| Package | Version | npm |
|---------|---------|-----|
| martin-loop | 0.3.12 | ✓ live |
| @martinloop/mcp | 0.3.5 | ✓ live (no bump needed) |

## Everything committed and pushed

Public repo: https://github.com/Keesan12/martin-loop (main, v0.3.12 tagged)
Private OSS: https://github.com/martin-Loop/ML_Core_OSS_Internal (main, v0.3.12 tagged)
Engine repo: https://github.com/martin-Loop/ML_Engine_Internal (main)
Control plane: https://github.com/martin-Loop/ML_Control_Plane_Internal (main)

## What was built this session

### Hard governance enforcement (0.3.12 — SHIPPED)
- `martin estimate` writes receipt to workflow-state.json (`cli.estimate` namespace)
- `martin gate` requires both `doctor` + `estimate` receipts to pass
- Gate reads from BOTH `cli` and `mcp` namespaces (merged)
- MCP `martin_run` blocks until estimate receipt exists
- `martin mcp install --host claude` auto-installs PreToolUse hooks into `~/.claude/settings.json`
- `installClaudeGovernanceHooks()` function in mcp-config.ts

### Memory store (built, in 0.3.12)
- `packages/core/src/persistence/memory-store.ts` — append-only JSONL
- Functions: appendMemory, readMemoryEntries, getPreference, buildMemorySummary, recordPreference, recordConsent
- MCP resource: `martin://agent/memory-summary`
- Exported from core index and discovery-metadata

### Cost-aware model routing (0.3.12)
- `selectModelTier(mode)` → haiku/sonnet/opus based on route
- `resolveModelForTier(tier, engine)` → concrete model IDs for 7 engine families
- `selectBestEngine(tier, availableEngines)` → picks cheapest available engine
- Estimate output shows: "haiku → claude-haiku-4-5-20251001 (saves ~$X vs sonnet)"

### Proactive onboarding (in 0.3.12)
- `martin start` detects IDE (Claude/Codex/Cursor/VS Code/Gemini via env vars)
- Shows MCP install command for detected host
- Shows estimate step before governed run
- `detectHostIDE()` function in index.ts

### Test integrity validator (in 0.3.12)
- `packages/core/src/test-integrity.ts` — detects circular/fake tests
- `packages/core/src/persistence/trace-store.ts` — append-only run history
- `packages/core/src/repo-analyzer.ts` — detects repo conventions → prompt directives

### OpenAI engine support (in 0.3.12)
- `openai` added to MARTIN_ENGINE_VALUES
- Doctor shows openai engine status from MARTIN_OPENAI_API_KEY
- Covers: DeepSeek, Qwen, Nemotron, Kimi, Llama via OpenAI-compatible endpoint

## REMAINING WORK

### Priority 1: Fix 5 — Install hooks into settings.json for this session
The user approved this but the hook was blocked by permission system (self-modification).
Next engineer needs to run:
```sh
martin mcp install --host claude --scope user
```
This now auto-writes the gate hook to ~/.claude/settings.json.

### Priority 2: Phase 3 Wave 2 (Plans 3+4) — NOT YET DONE
**Plan 3: Autonomous model switching in runMartin**
- In packages/core/src/index.ts (runMartin, ~line 487)
- classifyRoute() on task before adapter selection
- resolveModelForTier(route.recommendedModelTier, input.engine ?? 'claude')
- If no --model specified, use suggestedModel
- Log "MartinLoop auto-selected ${model}"

**Plan 4: Proactive consent prompting**
- In executeStartCommand: check memory for 'budget.default' preference
- If not stored, prompt user for budget
- Store in memory store

### Priority 3: MCP HTTP transport (Phase 4A)
- Add StreamableHTTPServerTransport to packages/mcp/src/server.ts
- `npx @martinloop/mcp --http --port 3033`
- Currently only stdio — blocks cloud/JetBrains/Jupyter hosts

### Priority 4: Phase 3 Wave 3 (Plan 5) — Tests
- `packages/core/tests/memory-store.test.ts` (8 tests)
- `packages/cli/tests/model-auto-selection.test.ts` (5 tests)
- Trace wiring smoke test

### Priority 5: Dashboard production wiring
- `ML_Control_Plane_Internal` — Next.js control plane
- Needs real API connections to run store
- Should match design mockups (user mentioned images were sent previously)

### Priority 6: GSD → MartinLoop paid rebrand
- `ML_Engine_Internal/packages/workflow-engine/` — DONE (14 tests)
- CLI commands: `martin plan`, `martin execute`, `martin verify` — NOT DONE
- Route through workflow-engine package

## Context from the master plan (.planning/phases/03-memory-autonomy/03-PLAN.md)

The full 5-plan Phase 3 plan is at:
`ML_Core_OSS_Internal/.planning/phases/03-memory-autonomy/03-PLAN.md`

Wave 1 (Plans 1+2) — DONE (memory store + trace wiring)
Wave 2 (Plans 3+4) — TODO (autonomous model switching + consent)
Wave 3 (Plan 5) — TODO (tests)

## Key files modified this session

- `packages/core/src/routing.ts` — selectModelTier, resolveModelForTier, selectBestEngine
- `packages/core/src/index.ts` — exports for all new modules
- `packages/core/src/test-integrity.ts` — NEW
- `packages/core/src/persistence/trace-store.ts` — NEW
- `packages/core/src/persistence/memory-store.ts` — NEW
- `packages/core/src/repo-analyzer.ts` — NEW
- `packages/cli/src/index.ts` — gate, estimate, start, gate enforcement
- `packages/cli/src/workflow-state.ts` — estimate step added
- `packages/cli/src/mcp-config.ts` — auto-install hooks, governance hook generation
- `packages/mcp/src/workflow-state.ts` — estimate TTL + gate step
- `packages/mcp/src/resources.ts` — governance-status + memory-summary resources
- `packages/mcp/src/prompts.ts` — auto-governance rules in kickoff
- `packages/mcp/src/server.ts` — martin_estimate tool
- `packages/mcp/src/discovery-metadata.ts` — new resources + martin_estimate in profiles

## How to start next session

1. Read this handoff
2. Run `martin doctor && martin estimate "<your task>" --budget-usd 5`
3. Wait for estimate output, then start work
4. Execute Phase 3 Wave 2 (Plans 3+4): autonomous model switching + consent prompting
5. Run `pnpm build && pnpm test` after each plan

## Tests passing
- CLI: 205+ tests
- Core: 25+ new tests (test-integrity, trace-store, repo-analyzer)
- MCP: 14 baseline tests
- All green as of this handoff
